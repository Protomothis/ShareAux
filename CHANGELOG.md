# Changelog

## v0.2.11

### 🛡️ 안정성 개선 (#48)

- 로그인 요청 제한이 설정값과 일치하도록 수정되었습니다
- 서버 내부 타입 검증이 강화되어 예상치 못한 오류가 줄어듭니다
- 설정값 접근 방식이 통일되어 환경 변수 누락 시 즉시 감지됩니다

### 🎨 UI 품질 개선

- 스타일 처리 방식이 통일되어 다크 모드 전환이 더 안정적입니다
- 일부 선택 메뉴가 앱 디자인에 맞게 교체되었습니다
- 모달 높이가 다양한 기기에서 더 정확하게 표시됩니다

### 🧹 코드 정리

- 사용하지 않는 코드가 제거되어 앱 로딩이 약간 빨라집니다
- 서버 내부 수치들이 설정 가능한 형태로 정리되었습니다

## v0.2.10

### 📊 차트 UI 마무리 (#46, #47)

- 차트 상위 곡이 컨테이너 너비에 맞게 배치됩니다 (가로 스크롤 제거)
- 모바일에서는 3열, 데스크톱에서는 5열로 반응형 표시
- 차트 목록에 숫자 페이지네이션이 추가되었습니다 (20곡씩)
- 추천곡과 라디오가 하나의 "추천" 탭으로 통합되었습니다

## v0.2.9

### 📊 차트 기반 추천 + 쇼케이스 개편 (#43)

- 인기 차트를 자동으로 수집합니다 (19개 장르/국가별 플레이리스트)
- 곡 추가 화면이 탭 기반으로 개편되었습니다 — 차트, 인기곡, 내 기록, 추천을 한 곳에서
- 차트 데이터가 없는 탭도 비활성 상태로 미리 볼 수 있습니다
- AI DJ가 차트 인기곡을 참고하여 더 정확한 추천을 합니다
- 새로운 AutoDJ 모드 "차트" 추가 — 인기 차트에서 직접 선곡
- 추천곡과 라디오가 하나의 "추천" 탭으로 통합되었습니다
- 서버 시작 시 차트 데이터가 비어있으면 즉시 수집합니다

### 🎨 UI/UX 개선

- 탭이 많아도 줄 바꿈으로 모두 표시됩니다 (가로 스크롤 제거)
- 탭 로딩 중 이모지 대신 스피너가 표시됩니다
- 탭을 다시 누르면 추천 목록이 새로고침됩니다
- 곡 길이 정보가 없는 트랙은 "0:00" 대신 숨깁니다
- 콘텐츠만 스크롤되고 탭 바는 항상 고정됩니다

### ⚙️ 어드민

- 차트 수집 활성화/시각 설정이 추가되었습니다

## v0.2.8

### 🛠️ 어드민 패널 품질 개선 (#45)

- 삭제/초기화 확인이 시스템 팝업 대신 앱 내 다이얼로그로 표시됩니다
- API 호출 방식이 통일되어 안정성이 향상되었습니다
- 하드코딩된 한국어 텍스트가 언어 설정에 따라 표시됩니다
- 설정 페이지의 타입 안전성이 강화되었습니다 (Gemini 모델 목록, 시크릿 상태)
- 접근성 개선 — 테이블, 페이지네이션, 체크박스에 스크린 리더 지원 추가
- 실시간 메트릭 차트의 렌더링 안정성이 개선되었습니다
- 서버 컨트롤러가 역할별로 분리되어 유지보수가 쉬워졌습니다

### 🏗️ 타입 시스템 통합

- 권한, 색상, 제공자 등 enum 타입이 하나로 통합되었습니다 (이전: DTO별 중복 생성)
- 불필요한 타입 캐스팅이 제거되어 코드 안정성이 향상되었습니다
- 자동 생성 타입 약 30개가 정리되었습니다

## v0.2.7

### 🎵 가사 번역 품질 개선

- 번역 톤이 곡 전체에서 일관되게 유지됩니다 (반말 서술체 통일)
- 일본어 곡의 발음 표기가 한글로 나옵니다 (기존: 로마자)
- 긴 가사도 앞뒤 문맥을 참고하여 자연스럽게 이어집니다
- 번역 누락 줄이 크게 줄었습니다

### 🔧 코드 품질 개선

- 버튼 스타일이 통일되었습니다 (디자인 시스템 정리)
- 방 화면에서 일부 영역 오류 시 나머지가 정상 동작합니다
- 플레이어/채팅/큐 영역이 더 빠르게 렌더링됩니다
- AutoDJ 탭 이름이 언어 설정에 따라 표시됩니다
- 접근성 개선 (스크린 리더 지원 강화)

### 🏗️ 내부 구조 개선

- 서버 이벤트 처리 구조 개선 (안정성 향상)
- 타입 안전성 강화 — 설정값 오류를 사전에 감지합니다

## v0.2.6

### 🐛 버그 수정

- AutoDJ 활성화 시 바로 곡이 추가되던 문제 수정 — 이제 패널에서 시작해야 동작합니다
- AI 모드에서 태그 조건이 무시되던 문제 수정
- 후보곡이 큐에 추가된 후에도 목록에 남아있던 문제 수정

### ✨ 개선

- AutoDJ 후보곡 추가/제거 시 부드러운 애니메이션 적용
- 후보곡이 위에서부터 순서대로 큐에 추가됩니다 (핀 고정 우선)
- 후보곡 추가/스킵 시 목록에서 즉시 사라집니다
- AI 추천 실패 시 채팅에 안내 메시지 표시 (3회 실패 시 자동 비활성화)
- AutoDJ 상태 표시(검색 중/추가 중)가 최소 2초 유지되어 더 잘 보입니다

### 🔧 안정성 개선

- 내부 오류 발생 시 로그 기록 강화 — 문제 추적이 빨라집니다
- 플레이어/채팅/큐 영역 중 하나에서 오류가 나도 나머지는 정상 동작합니다
- 가사 상태 표시가 더 정확해졌습니다 (synced/plain 구분)

## v0.2.5

### 🤖 AutoDJ 후보 미리보기

- 모든 모드에서 다음 후보곡 미리보기 지원 (AI 전용 → 전 모드 확장)
- 후보 항목에 즉시 큐 추가 버튼 (핀/추가/스킵)
- 곡 전환 시 자동 풀 갱신, 큐 변경 시 중복 자동 제거
- 스킵한 곡은 새로고침해도 재등장 방지
- 후보 Content ID 자동 매칭 — YouTube 제목 → 정식 곡명 자동 갱신
- AI 모드 검색 병렬화 (체감 속도 5배 개선)

### 🎛️ AutoDJ UX 개선

- 활성화 시 일시중지 상태로 시작 — 패널에서 설정 후 직접 시작
- 시작/적용/일시중지 버튼 통합 (상태별 자동 전환)
- 헤더 고정 + 스크롤 분리 (모드 선택은 항상 보임)
- 탭 바에 AutoDJ 상태 아이콘 표시 (재생 중/일시중지)
- 방 설정에서 "패널에서 시작해야 동작합니다" 안내

### 🏷️ AI DJ 태그

- 태그 label i18n 적용 (한국어/영어)
- 무드 +5, 장르 +7, 시대 +1, 국가 +3 추가
- 취향 슬라이더 추가 (대중적/중립/마이너)
- Gemini 프롬프트용 라벨 분리 — 영어 구체적 설명으로 추천 정확도 향상
- 어드민 태그 프리셋 관리 제거 — 코드 고정

### 🧹 코드 품질

- duration 매직넘버 → 상수 추출 (TRACK_MIN/MAX, AUTODJ_MIN/MAX)
- RangeSlider 공통 컴포넌트

## v0.2.4

### 🤖 AI AutoDJ

- 새로운 AutoDJ 모드: AI — Gemini가 분위기/장르/시대/국가 태그 기반으로 곡 추천
- 다음 후보 미리보기 — 핀(고정)/스킵으로 큐에 들어갈 곡 관리
- 후보가 큐에 추가되면 목록 자동 갱신
- 모드 변경 시 "적용" 버튼으로 확인 후 반영 (실수 방지)
- 설정 되돌리기 버튼 추가
- 일시중지/재개 토글
- 모드별 가이드 텍스트 표시
- AI 태그 선택 안내 문구
- 도움말(?) 팝오버

### 🌐 가사 번역 다국어 지원

- 번역 타겟 언어를 어드민에서 선택 가능 (12개 언어)
- 기존 한국어 고정 → 설정값 기반으로 변경
- 일본어 발음 가이드는 타겟이 한국어/영어일 때만 생성

### ⚙️ 어드민 설정

- AI DJ 섹션 추가 (활성화/모델/배치 사이즈/온도)
- 태그 프리셋 관리 — 서버 저장/조회 연동
- 번역 타겟 언어 드롭다운 추가

### 📱 레이아웃 개선

- RoomLayout 반응형 slot 패턴 도입 — 레이아웃 로직 한 곳에서 관리
- 모바일 AutoDJ 탭 연결 (데스크톱과 동일 기능)
- 곡 추가 모달 추천 탭 이중 스크롤 수정

### 🧹 코드 품질

- useAutoDj 훅 추출 — 모바일/데스크톱 API 로직 공유
- AutoDjTagsDto 공유 DTO로 추출 — 타입 캐스팅 제거
- TranslationLang enum 추가 (type-safe 언어 설정)
- AGENTS.md 업데이트 (공유 DTO, 컴포넌트 계층, 훅 패턴, Storybook 규칙)

### 🐛 버그 수정

- 방 설정에서 AI 모드 선택 시 잘못된 에러코드 표시
- AutoDJ 일시중지 토글 후 UI 미갱신
- 데스크톱 큐에서 AutoDJ 로딩 상태 미표시
- Gemini 키 없을 때 AI 모드 disabled 처리 (방 설정 + AutoDJ 탭)

## v0.2.3

### 🔊 Cast/AirPlay 안정화

- 모바일 Safari에서 Cast 버튼이 동작하지 않던 문제 수정
- Cast 연결 중 곡이 끝나거나 방이 종료되면 자동으로 연결 해제
- 스트리밍 중이 아닐 때 Cast 버튼 비활성화 (불필요한 에러 방지)
- Cast 연결 중 토큰 자동 갱신 (장시간 사용 시 끊김 방지)
- iOS 백그라운드 복귀 시 Cast 재생 상태 자동 복구

### 🎵 재생 정보 개선

- 코덱/비트레이트 태그가 곡 전환 시 즉시 반영 (기존: 새로고침 필요)
- 가사 검색 중/번역 중 상태를 태그로 실시간 표시
- 곡 전환 후 큐에서 신청자 정보가 사라지던 문제 수정

### 📱 모바일/UX

- 추천 탭에서 그리드/리스트 뷰 전환 지원
- 즐겨찾기 드래그 시 고스트가 잘리던 문제 수정
- 중첩 모달(폴더 관리) 백드롭/닫기 정상 동작
- 폴더 생성 시 Enter 두 번 눌리던 문제 수정
- 재생 중 표시 dot이 모바일에서 잘리던 문제 수정
- URL 입력 필드 좌우 패딩 정렬

### 🔔 알림

- 방 입장할 때마다 "알림 활성화" 푸시가 뜨던 문제 수정 (최초 등록 시에만)

### 🧹 코드 정리

- StreamTags 컴포넌트 추출 (재사용 가능)
- FavoritesList → FavItem, FolderSection 분리
- 공유 유틸 추출 (favToSearchResult)

## v0.2.2

### 🏗 내실 정리

- 모바일 Safari에서 Cast/AirPlay 버튼이 동작하지 않던 문제 해결
- 슬래시 명령어(`/ban`, `/mute` 등)가 동작하지 않던 문제 해결
- 채팅에서 `@닉네임` 멘션 시 푸시 알림 발송
- URL 가져오기 탭에 붙여넣기 버튼 추가 (iOS 클립보드 팝업 개선)
- 어드민 페이지 접근 권한 강화 (admin/superAdmin만 허용)
- 테스트 페이지 제거

### 🔧 코드 품질

- WebSocket 훅 분리 — 연결/메시지 라우팅/동기화 각각 독립 훅으로
- 서버 Gateway 3분할 — 연결 관리/메시지 처리/브로드캐스트 분리
- 재생 상태 훅 분리 — usePlaybackState, useRoomState 추출
- 프로젝트 전반 lint 에러 0개 달성 (서버 152→0, 클라이언트 8→0)
- ESLint 설정 정비 — 의도적 패턴 예외 처리
- 서버 stream Content-Type 수정 (`audio/aac` → `audio/mp4`)
- 매직 넘버 제거 — WsOpCode/WsCloseCode 상수 일관 사용
- HTTP 환경 clipboard API 에러 수정

---

## v0.2.1

### 🔊 외부 기기 재생 (Cast / AirPlay)

- HTTP 스트림 엔드포인트 추가 — 외부 기기에서 방 오디오 재생 가능 (#38)
- Chrome: Cast 버튼으로 Chromecast 연결 (Remote Playback API)
- Safari: AirPlay 버튼으로 HomePod/Apple TV 등 연결
- Cast 연결 시 로컬 오디오 자동 음소거, 버튼 재클릭으로 해제
- 기기에 곡 제목/아티스트/썸네일 표시 (Media Session API)
- 스트림 전용 토큰 인증 — 쿠키 의존 없이 모든 환경에서 동작
- 기존 ffmpeg에 ADTS output 통합 (fd3) — 라이브 시각 완벽 동기화

### 🐛 버그 수정

- 재생 불가 곡 스킵 시 시스템 메시지가 채팅에 표시되지 않던 문제 해결

### 🛠 개선

- dev.sh 전면 재작성 — 크로스 플랫폼 (macOS/Linux/Windows WSL)
- dev.sh `--https` 모드 추가 (Cast/AirPlay 로컬 테스트용)
- 개발 환경에서 LAN IP 접속 허용 (WS origin 체크 스킵)
- 미사용 AudioControl 컴포넌트 정리

---

## v0.2.0

### 🔗 URL 가져오기

- 신청하기 모달에 "URL 가져오기" 탭 추가 (#37)
- 영상/재생목록 URL 붙여넣기 → 자동 판별 → 곡 추가 또는 즐겨찾기 저장
- 뮤직 플랫폼 재생목록, Shorts, Mix URL 지원
- Mix(라디오) URL은 단일 영상으로 자동 폴백
- 클립보드 자동 감지 — 탭 진입 시 URL이 있으면 input에 채움
- 사용 불가 트랙(비공개/지역제한) 표시

### 💬 채팅 슬래시 명령어 + 멘션

- `/ban`, `/unban`, `/mute`, `/unmute`, `/clear` 호스트 전용 명령어 (#11)
- `@닉네임` 멘션 — 팝오버에서 유저 선택, 채팅에 하이라이트 표시
- 자기 멘션 시 배경색 강조
- `/clear` — 채팅 초기화 (모든 참여자에게 반영)
- 키보드 탐색 (↑↓ Enter Esc)

### 🐛 버그 수정

- 큐 드래그 순서 변경 시 곡 사라지는 문제 해결
- 로그인 후 페이지 이동 안 되는 문제 해결 (쿠키 반영 타이밍)
- manifest.json이 인증에 걸리던 문제 해결

### 🛠 개선

- Button 컴포넌트에 `loading` prop 추가
- Input 컴포넌트에 `clearable` prop 추가 (X 버튼)
- SearchTrackItem에 `unavailable` 상태 지원

---

## v0.1.17

### 🚀 단일 포트 배포

- Caddy 내부 게이트웨이 도입 — 포트 하나만 열면 프론트/API/WebSocket 전부 동작 (#35)
- 리버스 프록시에서 path 라우팅 설정 불필요 — 외부에서 단일 포트로 통째로 넘기면 끝
- 기존: server(3000) + client(3001) 각각 노출 → 변경: gateway(8080) 하나만 노출

### 🐛 버그 수정

- Docker 빌드 시 `@parcel/watcher` 누락으로 클라이언트 이미지 빌드 실패하던 문제 해결 (#36)

---

## v0.1.16

### 🔒 보안

- 서버 보안 취약점 6건 해결 (npm audit fix)
- 클라이언트 Next.js 16.2.5 업데이트 — 고위험 취약점 패치

### 🛠 안정성 개선

- 프리로드 동시 다운로드 슬롯 카운트 오류 수정 — 재시도 시 슬롯이 음수로 빠지던 문제 해결
- 비정상 종료된 방의 채팅 기록이 메모리에 남는 문제 수정

### 📦 의존성

- NestJS 11.1.19 등 서버 패키지 마이너 업데이트
- 코드 품질: 서버/클라이언트 lint 에러 전량 해결 (미사용 import, 타입 일관성 등)

---

## v0.1.15

### 📻 라디오 모드

- 신청하기에 라디오 섹션 추가 — 비슷한 아티스트의 곡을 자동으로 찾아 추천 (#34)
- AutoDJ 라디오 모드 — 현재 곡과 비슷한 아티스트 기반으로 자동 큐 추가
- 라디오 결과 5분 캐시 — 같은 곡 재생 중 반복 조회 시 빠른 응답

### 🔗 신청하기 개선

- 관련곡 / 라디오 섹션 분리 — 각각 독립 로딩, 새로고침 가능
- AutoDJ 모드 선택 시 각 모드별 설명 텍스트 표시

### 🔔 푸시 알림

- 곡 변경 알림에 곡명이 `{trackName}`으로 표시되던 문제 수정

### 🐛 버그 수정

- 큐에서 곡 순서 변경(드래그) 시 곡이 사라지는 문제 수정
- Admin 페이지 타입 에러 수정

### 🛠 개발 환경

- CI에 타입 체크 워크플로우 추가 — PR/push 시 자동 검증
- TrackVoteButtons 스토리북 복원

---

## v0.1.14

### 🎵 가사 번역

- 연속 번역 품질 개선 — temperature 고정, 동시 처리 수 조절, 청크 간 문맥 연결 (#31)

### 🔔 푸시 알림

- Mac Chrome 등에서 곡 변경 알림에 곡명/아티스트가 표시되지 않던 문제 수정 (#32)

### 🐛 버그 수정

- 게스트로 방 생성 시 즐겨찾기 API 불필요 호출 방지 (#33)
- 세션 만료 시 브라우저 "나가시겠습니까?" 확인 팝업 제거
- 토스트 알림에 닫기 버튼 추가 + 동일 에러 중복 표시 방지

---

## v0.1.13

### 🎵 가사 & 번역

- 가사 번역 안정성 대폭 개선 — 긴 가사 분할 번역, 실패 시 자동 재시도, 서버 재시작 후 미완료 번역 복구
- 번역 병렬 처리 — 동시 3곡까지 번역 가능 (기존 1곡 직렬)
- AI 응답 파싱 강화 — 다양한 형식 변형에 대응
- YouTube 자동 자막 폴백 제거 — 품질 낮은 음성 인식 자막 대신 전문 가사 소스만 사용
- LRCLIB 가사 매칭률 향상 — duration 허용 오차 확대

### 🔍 메타데이터 매칭

- 재생 시 Content ID 재시도 — 이전에 매칭 실패한 곡도 재생할 때마다 다시 시도
- YouTube Music Art Track(ATV) 메타 신뢰 — 앨범 기반 자동 생성 영상의 정확한 메타데이터 활용

### 📖 Storybook

- 컴포넌트 스토리 12개 → 28개로 확장 (Primitives 10개, Features 6개 신규)
- Storybook 다크 테마 통일 + 번역/상태관리 provider 자동 적용

### ⚡ 성능 & 인프라

- 서버 Docker 이미지 경량화 — multi-stage 빌드로 소스코드/devDeps 제거
- GitHub Actions 빌드 캐시 활성화 — 이미지 빌드 시간 단축
- 프로덕션 DB 자동 스키마 변경 방지 — synchronize 개발 환경 전용으로 분리

### 🛠 코드 품질

- Admin 모듈 분리 — 710줄 단일 서비스를 도메인별 4개 파일로 분리
- innertube-parser 타입 안전성 — any 16곳 제거, YouTube API 인터페이스 정의
- 인라인 HTML 요소 → shadcn 프리미티브 전환
- 핵심 서비스 유닛 테스트 47개 추가

---

## v0.1.12

### 🔔 푸시 알림

- iOS Safari에서도 푸시 알림 수신 가능

### 🎵 메타데이터 매칭

- MusicBrainz 폴백 매칭 정확도 개선

### 🐛 버그 수정

- mute/unmute 시 권한 체크 누락 수정
- WebSocket 연결 해제 시 메모리 누수 수정

### ⚡ 성능 개선

- 서버 N+1 쿼리 최적화 — 방 목록·멤버 조회 속도 개선
- 재신청 쿨다운 체크를 배치 쿼리로 전환
- DB 인덱스 추가 (자주 조회되는 컬럼)
- 어드민 차트 lazy load + 아바타 캐시 LRU 제한

---

## v0.1.11

### 🔔 Push 알림

- 곡 변경, 투표 스킵, 호스트 변경, 추방 시 Push 알림 (#10)
- 곡 변경 알림에 썸네일 + ⭐ 즐겨찾기 액션 버튼
- 알림 클릭 시 해당 방 탭으로 포커스
- 방별 알림 음소거 + 트리거별 ON/OFF 설정
- 알림 설정: 방 내 벨 아이콘 + 프로필 설정
- 브라우저 권한 상태 표시 + OS 알림 차단 안내
- 구독 등록 시 테스트 알림 발송 (파이프라인 검증)
- VAPID 키 자동 생성 (설정 불필요)

### 🎵 메타데이터 매칭

- MusicBrainz title-only 검색 시 score 95 + duration 5초로 오매칭 방지 강화

### ⚙️ 인프라

- NestJS 라이프사이클 개선 — onReady 패턴 제거, OnApplicationBootstrap 전환
- 개발 환경 전용 TestModule (토큰 발급 + Push 테스트)
- Service Worker 버전 package.json 연동

---

## v0.1.10

### 🔍 검색 개선

- 검색 결과가 재생목록 / 일반 섹션으로 분리 — 접기/펼치기 가능 (#28)
- 재생목록 최대 표시 수 5 → 20개로 확대
- 쇼케이스(인기/내 기록/추천) 섹션도 접기/펼치기 지원
- 쇼케이스 카드 크기 통일 — 제목 길이와 무관하게 정렬

### 🎨 디자인 개선

- 방 카드 리디자인 — 재생 중 accent glow, 멤버 아바타 스택, 배경 블러 강화
- DESIGN.md 스펙 준수 (패딩/간격/그림자)

### 🎵 메타데이터 매칭 강화

- YouTube Music (WEB_REMIX) OMV 메타데이터 폴백 추가
- 매칭 순서: Content ID → YouTube Music OMV → MusicBrainz → 미매칭
- Content ID 매칭 상태 3단계 전환 (대기/매칭됨/미매칭)
- 트랙 관리 목록에 Content ID 매칭 상태 컬럼 추가

### 🌐 번역 개선

- 시스템 메시지(채팅 제한/추방/권한 변경 등) 한국어 하드코딩 제거 → i18n 전환
- 관리자 설정 API 키 입력 필드 i18n 적용
- 이벤트 코드 문자열 비교 → WsEvent enum 전환

### 🐛 버그 수정

- 모바일 백그라운드 복귀 시 먹통/검은 화면 방지 (#29)
- 좋아요/싫어요 버튼 렌더링 시 애니메이션 자동재생 방지
- 듣는 중 실수로 페이지 이탈 방지 (beforeunload 가드)
- 게스트 유저 Role 변경 차단

### ⚙️ 인프라

- GHCR 빌드 후 untagged 이미지 자동 삭제

---

## v0.1.9

### ⚙️ 관리자 시스템 설정

- 관리자 페이지에서 시스템 설정을 실시간으로 변경 가능 (#18)
- Google 로그인, 캡차, Gemini 번역 등 API 키를 웹에서 관리
- API 키는 암호화 저장 — 관리자 화면에서도 마스킹 표시
- 설정 변경 시 서버 재시작 없이 즉시 반영

### 🚀 초기 설정 온보딩

- 첫 설치 시 3단계 안내 화면 제공 (계정 생성 → 초대 코드 → 완료) (#25)
- 초대 링크 URL 복사 기능 추가
- 기본 권한이 전체 선택으로 변경

### 🎨 디자인 통일

- 카드, 스켈레톤, 빈 상태 등 반복 패턴을 공통 컴포넌트로 통일 (#23)
- 전체 페이지에 일관된 스타일 적용

### 🌐 번역 개선

- 방 화면, 모바일 탭, 에러 메시지 등 누락된 번역 추가 (#14)
- API 에러 메시지가 언어에 맞게 자동 번역
- Gemini 모델 목록에서 안정/프리뷰 모델만 표시
- 서버 시작 시 번역 서비스 초기화 순서 보장

### 🐛 버그 수정

- AutoDJ 설정 변경 시 불필요한 시스템 메시지가 표시되던 문제 수정 (#22)

---

## v0.1.8

### 🌐 다국어 지원 (i18n)

- 한국어 / 영어 전체 지원 — 브라우저 언어에 따라 자동 감지 (#14)
- 모든 UI 텍스트, 버튼, 안내 메시지가 선택한 언어로 표시
- 관리자 페이지(대시보드, 유저/방/트랙 관리, 설정 등) 전체 다국어 대응
- API 에러 메시지도 언어에 맞게 표시 (70개 에러 코드 번역)
- 시스템 채팅 메시지(입장/퇴장/곡 추가/스킵 등) 다국어 표시
- 개인정보처리방침, 이용약관 페이지 언어별 분리
- 브라우저 탭 타이틀이 각 페이지에 맞게 표시

### 📝 문서

- README 영어/한국어 분리 — 상단 언어 스위처로 전환
- 기능 상세, 배포 가이드, 개발 가이드, FAQ, 아키텍처 문서 영어 번역 추가
- 환경 변수 설명(.env.example) 한국어/영어 병기

### 🎵 시스템 메시지 개선

- 유저가 곡을 추가했을 때와 AutoDJ가 추가했을 때 메시지가 구분되어 표시
- 시스템 메시지에 닉네임, 곡 이름 등 상세 정보가 정확히 표시

---

## v0.1.7

### 🎵 재생 품질 개선

- 곡 시작 시 처음부터 정상 재생 (이전에는 3초 뒤부터 재생되던 문제 수정) (#13)
- 재생 중 반복 끊김 현상 해결 — 네트워크 불안정 시 자동으로 버퍼를 다시 확보한 뒤 재생 (#13)
- 곡 전환 시 끝부분이 잘리지 않도록 서버가 충분히 대기 후 다음 곡으로 전환 (#13)
- 곡 전환 시 경과 시간이 순간적으로 이전 곡 시간으로 표시되던 문제 수정 (#8)
- 재생 시작까지 걸리는 시간 단축 (3초 → 2초) (#13)
- 버퍼링 중 재생 버튼에 로딩 표시 (#13)

### 🔍 검색 & 곡 정보

- 곡 정보(아티스트, 앨범) 매칭 정확도 대폭 향상 — 11% → 84% (#16)
- 검색 결과 무한스크롤이 동작하지 않던 문제 수정 (#20)
- 검색 재생목록 내 트랙에 즐겨찾기 버튼이 표시되지 않던 문제 수정 (#12)

### 🔒 보안

- WebSocket 연결 시 출처(Origin) 검증 추가 (#15)
- Swagger API 문서가 운영 환경에서 노출되지 않도록 차단 (#15)
- 비인증 상태의 대량 WebSocket 연결 시도 자동 차단 (#15)
- 인증 쿠키 구조 개선 — 민감 정보를 httpOnly JWT 기반으로 전환 (#21)
- 채팅 메시지에서 HTML 태그 문자 제거 (#15)

### 🛠 에러 처리 & 관리

- 로그인/회원가입 시 에러 메시지가 정확한 입력 필드에 표시 (#17)
- 어드민 인기 트랙 목록에 페이지네이션 추가 — 전체 트랙 관리 가능
- 어드민 트랙 목록에서 매칭된 곡명/아티스트명 우선 표시 (#16)

### 📱 모바일

- 모바일에서 백그라운드 전환 시 방이 삭제되던 문제 완화 — 재연결 대기 시간 확대 (#9)

### 🧹 기타

- 오디오 리소스 누수 방지 — 방 퇴장 시 정리 (#19)
